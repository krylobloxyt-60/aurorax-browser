# AuroraX Browser

AuroraX is a Windows desktop browser concept by Aurora Labs, built with Electron.

## Run locally

```bash
npm install
npm start
```

## Build the Windows installer

```bash
npm run dist
```

## Deploy the AuroraX web page

```bash
npm run web:deploy
```

The website lives in `aurorax-web/`.

